[package]
name = "vicissitude-hua"
version = "0.1.0"
edition = "2024"

[dependencies]
url = "2.5.4"
reqwest = { version = "0.12", features = ["json"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] } # 提供 `Serialize` 和 `Deserialize` 宏
serde_json = "1.0"  # JSON 支持
regex = "1.9"  # 最新稳定版
