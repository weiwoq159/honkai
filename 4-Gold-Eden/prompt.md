这是我的公开 Git 仓库地址：

https://github.com/weiwoq159/honkai/tree/master/4-Gold-Eden

我这次要解决的问题是：

{具体问题描述}

请你先基于仓库里的真实代码分析，不要直接凭空给方案。

请优先查看这些相关位置：

我不确定，你先自行判断相关文件

我的期望结果是：

给出可复制代码

请按下面格式回答：

1. 你先看到的相关代码结构是什么
2. 当前问题最可能出在哪里
3. 应该改哪些文件，为什么
4. 推荐的最优改动方案
5. 给出可直接复制的代码
6. 如果还缺信息，请明确告诉我要补充哪个文件或日志

要求：

- 基于仓库真实代码分析
- 不要恭维，不要安慰，直接指出问题所在
- 不要为了省事给临时补丁，要给长期更合理的最优方案
- 不要过度设计，但要考虑后续维护、扩展、插件下载和多开发者协作
- 如果涉及 Tauri / Rust / React / Python 插件，请说明三端分别要改什么
- 如果涉及路径问题，请考虑换电脑、换目录、插件下载后的兼容性
- 如果涉及 Python 插件，请保证 stdout 只输出最终 JSON，日志走 logs 字段
- 如果存在多个方案，请对比优缺点，并明确推荐哪一个
- 如果当前代码设计方向不对，请直接指出，不要只在现有错误上打补丁
- 如果需要新增文件，请明确说明文件路径、文件名、职责，以及为什么不能放在已有文件里
- 如果需要删除或废弃旧代码，请明确指出哪些旧文件/函数不再使用，避免新旧逻辑并存
- 如果涉及配置文件，请说明配置项含义、默认值、是否需要提交到 Git
- 如果涉及安全信息，例如 token、密码、cookie、密钥，请明确说明哪些不能打印、不能提交、不能暴露到前端
- 如果涉及多环境运行，请考虑开发环境、打包环境、用户安装目录、插件下载目录的差异
- 如果涉及路径，请避免依赖当前工作目录，优先使用应用根目录、插件根目录或配置目录推导
- 如果涉及 Python 子进程，请明确 stdin、stdout、stderr 分别应该承载什么
- 如果涉及日志，请区分前端展示日志、调试日志、错误栈日志，不要混用 print
- 如果涉及长期架构，请同时给出当前阶段该做什么、暂时不该做什么
- 如果有破坏性改动，请提前说明影响范围和迁移步骤
